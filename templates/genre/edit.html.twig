{% extends 'base.html.twig' %}

{% block title %}Modifier le genre {{genre}}{% endblock %}

{% block body %}
    <h1>Modifier le genre {{genre}}</h1>

    {{ include('genre/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('genre_index') }}">Retourner à la liste</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
