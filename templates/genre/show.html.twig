{% extends 'base.html.twig' %}

{% block title %}{{genre}}{% endblock %}

{% block body %}
    <h1>{{genre}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}">Retourner à la liste</a>

    <a href="{{ path('genre_edit', {'id': genre.id}) }}">Modifier</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
