<div class="card m-4">
    <img class="card-img-top" src="{{ vich_uploader_asset(movie, 'imageFile') }}" alt="Card image cap">
    <div class="card-body">
        <h4 class="card-text text-center">{{ movie.name }}</h4>
        <div class="row">
            <a href="{{ path('movies_show', {'id': movie.id}) }}"
               class="btn btn-outline-primary mr-3 ml-1 mx-auto">Voir plus</a>
        </div>

    </div>
</div>