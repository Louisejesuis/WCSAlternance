<nav class="navbar navbar-expand-sm navbar-dark bg-primary fixed-top">
    <a class="navbar-brand" href="{{ path('index') }}">Wild Ciné</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <div class="d-flex flex-row-reverse">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_USER') %}

                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="{{ path('movies_new') }}">Créer un film</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="{{ path('people_new') }}">Créer une personne</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="{{ path('genre_new') }}">Créer un genre</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('security_logout') }}" class="nav-link btn btn-primary">Se deconnecter</a>
                </li>
            {% else %}
                <li class="nav-item mx-3">
                    <a href="{{ path('security_login') }}" class="nav-link btn btn-primary">Se connecter</a>
                </li>
                <li class="nav-item mx-3">
                    <a href="{{ path('user_registration') }}" class="nav-link btn btn-primary">Créer un compte</a>
                </li>

            {% endif %}
        </ul>
        </div>
    </div>
</nav>